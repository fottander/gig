<div class="profile-<%= @profile.id %>">
  <div class="card-profile-stats">
    <div class='breadcrumb-holder-smaller'>
        <%= render_breadcrumbs separator: ' > ' %>
    </div>
    <div class="card-profile-stats-intro">
      <%= image_tag @profile.avatar.url, class: 'card-profile-stats-intro-pic' %>
      <div class="card-profile-stats-intro-content">
        <h2><%= @profile.username %></h2>
        <% if current_company.present? %>
          <%= link_to 'Bjud in till jobb', new_invite_path(profile_id: @profile.id), class: 'profile-button-small' %>
        <% end %>
        <% if @invoices.present? %>
          <p class='job-count'>Antal genomförda jobb: <%= @invoices.count %></p>
          <p class='job-count'>Totalt fakturerat:
            <% if (@invoices.sum('amount') * 1.25).round < 10000 %>
              0 - 10 000 kr
            <% elsif (10000..15000).include? (@invoices.sum('amount') * 1.25).round %>
              10 000 - 15 000 kr
            <% elsif (15001..25000).include? (@invoices.sum('amount') * 1.25).round %>
              15 000 - 25 000 kr
            <% elsif (25001..50000).include? (@invoices.sum('amount') * 1.25).round %>
              25 000 - 50 000 kr
            <% elsif (50001..100000).include? (@invoices.sum('amount') * 1.25).round %>
              50 000 - 100 000 kr
            <% elsif (100001..249999).include? (@invoices.sum('amount') * 1.25).round %>
              100 000 - 250 000 kr
            <% elsif (@invoices.sum('amount') * 1.25).round > 249999 %>
              Mer än 250 000 kr
            <% end %>
          </p>
        <% end %>
      </div>
      <p><%= @profile.description %></p>
    </div>

    <hr />

    <div class="card-profile-stats-container">
      <div class="large-2 columns card-profile-stats-statistic">
        <span class="stat">Titel</span>
        <p><%= @profile.title %></p>
      </div>
      <div class="large-3 columns card-profile-stats-statistic">
        <span class="stat">Färdigheter</span>
        <p><%= @profile.skill %></p>
      </div>
      <div class="card-profile-stats-statistic">
        <span class="stat">Stad</span>
          <p><%= @profile.cities.pluck(:name).flatten.join(' & ') %></p>
      </div>
      <div class="card-profile-stats-statistic">
        <span class="stat">Kategorier</span>
        <p><%= @profile.categories.pluck(:name).flatten.join(' & ') %></p>
      </div>
      <div class="card-profile-stats-statistic">
        <span class="stat">Lön</span>
        <p><%= @profile.rate %></p>
      </div>
    </div>
  </div>
</div>
<% if @invoices.present? %>
  <div class="card profile-card-action-icons">
    <div class="card-section">
        <div class="profile-card-header">
          <h4 class='feedback-title'>Feedback</h4>
        </div>
        <div class="profile-card-about-feed">
          <% @invoices.each do |invoice| %>
            <% if invoice.feedback? %>
              <div class='profile-card-about-title-message'>Jobb ID: <%= invoice.job_id %></div>
              <div class="profile-card-about-feed-message">
              <%= invoice.updated_at.strftime("%Y %B") %><br>
              <%= invoice.feedback %>
              </div>
            <% end %>
          <% end %>
        </div>
    </div>
  </div>
<% end %>
