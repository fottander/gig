<div class="holy-grail-grid">
  <div class="holy-grail-left-application">
    <div class='holy-grail-left-application-box'>
      <div class='holy-grail-left-application-box-title'>
        TIPS FÖR EN BRA ANSÖKAN <br>
      </div>
      Hur utförlig ditt meddelande skrivs och hur bra din profil matchar jobbet ökar dina chanser för att få jobbet. <br>
      Kontaktinformation ska ej finnas med i ansökan.
    </div>
  </div>
  <div class="holy-grail-middle">
    <div class='breadcrumb-holder-small'>
        <%= render_breadcrumbs separator: ' > ' %>
    </div>
    <div class="simple-form">
      <div class='post-title'><h3>Ändra ansökan</h3></div>
      <% if current_user.profile.present? %>
        <% if current_user.profile == @application.profile %>
          <%= form_for ([@job, @application]) do |f| %>
            <% if @application.errors.any? %>
              <% @application.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            <% end %>

            <div class="fieldContainer">
              <%= f.label :message, 'Öka dina chanser genom att utöka din ansökan med ett kort meddelande till företaget (frivilligt)', class: 'form-text' %>
              <%= f.text_area :message, placeholder: 'Skriv ett kortfattat meddelande' %>
              <div class="fieldInfo">
                Tips <br>
                I din ansökan kommer din profil(användarnamn, titel, beskrivning etc.) att bifogas. Skriv därför ett meddelande baserat just på det jobb du söker till. Håll utkik i din kontrollpanel och mailkorg för svar på ansökan.
              </div>
            </div>
            <%= f.submit 'Ändra', class: 'reply-button' %>
            <%= link_to 'Radera ansökan', job_application_path(@job, @application), data: { confirm: "Är du säker?" }, method: :delete, class: 'profile-button' %>
          <% end %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
