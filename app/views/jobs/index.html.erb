<h2>Browse all jobs</h2>
<% @jobs.each do |job| %>
<div class="card profile-card-action-icons">
    <div class="card-section">
        <div class="profile-card-header">
            <div class="profile-card-author">
                <h5 class="author-title"><%= link_to job.title, job_path(job)%></h5>
                | Skapad: <%= job.created_at.strftime("%Y-%m-%d") %> | Går ut: <%= job.deadline %> |
            </div>
        </div>
        <div class="profile-card-about">
          Beskrivning: <%= job.description %> <br>
          | Stad: <%= job.city %> | Kategorier: <%= job.category %> | Krav: <%= job.requirement %> |<br>
          | Varaktighet: <%= job.duration %> | <%= job.budget %> | Timmar/vecka: <%= job.hour_week %> |<br>
        </div>
    </div>
</div>
<% end %>
